<h1>Flats</h1>
<ul>
  <% @flats.each do |flat| %>
    <li><%= link_to flat["name"], flat_path(flat["id"]) %></li>
    <li><%= flat["price"] %> <%= flat["priceCurrency"] %></li>
    <br>
  <% end %>
</ul>

 <!-- <%#= link_to "home", root_path %> -->
